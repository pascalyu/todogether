<form method="POST" action="{{ path('task_done', {'id': task.id}) }}" onsubmit="">
	<input type="hidden" name="_method" value="POST">
	<button class="btn btn-success">
		<i class="fas fa-check"></i>
	</button>
</form>
